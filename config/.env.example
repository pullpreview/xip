# Root zone served by xip.
XIP_DOMAIN=pullpreview.com

# A records returned for the root domain.
XIP_ROOT_ADDRESSES=203.0.113.10

# A records used by ns-0, ns-1, ... hostnames.
XIP_NS_ADDRESSES=203.0.113.10

# SOA serial.
XIP_TIMESTAMP=0

# TTL for all generated records.
XIP_TTL=300

# Listener addresses.
XIP_LISTEN_UDP=:53
XIP_LISTEN_TCP=:53
XIP_LISTEN_HTTP=:80
XIP_LISTEN_HTTPS=:443

# CSV blocklist used for disabled hostnames.
XIP_BLOCKLIST_PATH=/etc/xip/blocklist.csv
XIP_BLOCKLIST_RELOAD_INTERVAL=60s

# Let's Encrypt configuration used by HTTPS listener.
XIP_ACME_CACHE_DIR=/etc/xip/acme-cache
# XIP_ACME_EMAIL=support@pullpreview.com

# Optional: enable HTTP redirect service (port set by XIP_LISTEN_HTTP).
# XIP_ROOT_REDIRECT_URL=https://pullpreview.com/?ref=xip

# Optional: OTEL metric export using standard OTLP env vars.
# If scheme is omitted, xip defaults it to https://
# OTEL_EXPORTER_OTLP_ENDPOINT=ingest.eu.signoz.cloud
# OTEL_EXPORTER_OTLP_HEADERS=signoz-ingestion-key=<your-ingestion-key>
# Optional signal overrides:
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=ingest.eu.signoz.cloud
# OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=ingest.eu.signoz.cloud
